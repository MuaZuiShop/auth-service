spring:
  application:
    name: auth-service
  config:
    import:
    - optional:file:.env[.properties]
    - 'classpath:web.yml'
    - 'classpath:eureka.yml'
    - 'classpath:database.yml'
logging:
  level:
    root: debug