spring:
  application:
    admin:
      default:
        username: admin
        password: admin
        email: admin@admin.com
  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    root: ${LOGGING_LEVEL:info}