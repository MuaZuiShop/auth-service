spring:
  application:
    admin:
      default:
        username: admin
        password: admin
        email: admin@admin.com
  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    root: info

jwt:
  secret: defaultDevSecretKeyThatIsLongEnough_please_replace
  access-exp: 900000
  refresh-exp: 604800000